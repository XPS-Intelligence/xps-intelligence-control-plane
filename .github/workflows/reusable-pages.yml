name: reusable-pages

on:
  workflow_call:

jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pages placeholder
        run: echo 'Reusable Pages workflow ready'
